# SyncWatch - Система синхронного просмотра медиаконтента

Платформа для совместного просмотра видео в режиме реального времени с синхронизацией воспроизведения, текстовым и голосовым чатом.

## Ключевые особенности

- **Синхронное воспроизведение** - все участники видят видео одновременно с точностью до 300мс
- **Без регистрации** - создание и присоединение к комнатам по прямой ссылке
- **Несколько источников медиа** - поддержка YouTube, VK Video, прямых URL и загруженных файлов
- **Текстовый чат** - общение в реальном времени
- **Голосовой чат** - WebRTC P2P аудиосвязь
- **Работа за NAT** - не требует проброса портов
- **Приватные комнаты** - защита паролем

## Архитектурные принципы

### Инварианты системы

1. **Сервер не воспроизводит видео** - сервер хранит только состояние воспроизведения
2. **Сервер - единственный источник истины** - все изменения проходят через сервер
3. **Видео загружается клиентом** - каждый участник получает медиа из CDN/источника
4. **Только хост управляет воспроизведением** - команды принимаются только от создателя комнаты

### Синхронизация

Синхронизация достигается через:

```
T = offset + (now_client - serverTimestamp) * rate
```

Где:
- `offset` - позиция в видео на момент последней команды
- `serverTimestamp` - серверное время последней команды
- `rate` - скорость воспроизведения (0.5x - 2x)

**Политика коррекции drift:**
- `|drift| < 100мс` → без действий
- `100мс ≤ |drift| < 300мс` → плавная коррекция через playbackRate
- `|drift| ≥ 300мс` → жесткий seek

## Документация

Проект содержит полную техническую документацию:

### 📋 [TECHNICAL_SPECIFICATION.md](./TECHNICAL_SPECIFICATION.md)
Техническое задание с детальным описанием:
- Требований (функциональных и нефункциональных)
- Архитектуры системы
- Моделей данных
- API и протоколов
- Метрик и критериев приемки

### 🛠️ [IMPLEMENTATION_PLAN.md](./IMPLEMENTATION_PLAN.md)
Подробный план реализации (часть 1):
- Подготовка проекта и структура
- Серверная инфраструктура (WebSocket, менеджеры)
- Клиентское приложение (UI, роутинг)
- Синхронизация воспроизведения (адаптеры, sync engine)

### 🛠️ [IMPLEMENTATION_PLAN_PART2.md](./IMPLEMENTATION_PLAN_PART2.md)
Подробный план реализации (часть 2):
- Текстовый чат
- Голосовой чат (WebRTC)
- Дополнительные функции (приватные комнаты, передача прав хоста)
- Оптимизация и тестирование
- Развертывание (Docker, nginx)

### 🎨 [DESIGN_SPECIFICATION.md](./DESIGN_SPECIFICATION.md)
Спецификация дизайна фронтенда (Apple Liquid Glass):
- Принципы дизайна Liquid Glass
- Цветовая система и типографика
- Материалы и эффекты (glassmorphism)
- Компоненты UI (кнопки, карточки, модалы)
- Анимации и переходы
- Готовый код CSS/Tailwind

## Быстрый старт

### Требования

- Node.js 20+
- npm или yarn
- Docker (опционально)

### Разработка

#### 1. Установка зависимостей

**Сервер:**
```bash
cd server
npm install
cp .env.example .env
```

**Клиент:**
```bash
cd client
npm install
```

#### 2. Запуск в режиме разработки

**Сервер:**
```bash
cd server
npm run dev
```

**Клиент:**
```bash
cd client
npm run dev
```

Приложение будет доступно на:
- Клиент: http://localhost:5173
- WebSocket сервер: ws://localhost:3001

### Production

#### Запуск через Docker

```bash
# Сборка и запуск
docker-compose up --build

# В фоновом режиме
docker-compose up -d

# Остановка
docker-compose down
```

Приложение будет доступно на http://localhost

## Структура проекта

```
sync-watch/
├── server/                 # Серверная часть (Node.js + TypeScript)
│   ├── src/
│   │   ├── core/          # Основная логика (RoomManager, StateManager)
│   │   ├── ws/            # WebSocket обработчики
│   │   ├── types/         # TypeScript типы
│   │   └── utils/         # Утилиты
│   └── package.json
│
├── client/                # Клиентская часть (React + TypeScript)
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── services/      # WebSocket, WebRTC менеджеры
│   │   ├── hooks/         # React хуки
│   │   └── types/         # TypeScript типы
│   └── package.json
│
├── docker/                # Docker конфигурация
│   ├── Dockerfile.server
│   ├── Dockerfile.client
│   └── nginx.conf
│
├── docs/                  # Документация
│   ├── TECHNICAL_SPECIFICATION.md
│   ├── IMPLEMENTATION_PLAN.md
│   └── IMPLEMENTATION_PLAN_PART2.md
│
├── docker-compose.yml
└── README.md
```

## Технологический стек

### Серверная часть

- **Runtime:** Node.js 20+
- **Язык:** TypeScript 5+
- **WebSocket:** ws
- **HTTP:** Fastify (опционально)
- **Логирование:** winston
- **Валидация:** zod

### Клиентская часть

- **Фреймворк:** React 18+
- **Язык:** TypeScript 5+
- **Роутинг:** react-router-dom
- **WebRTC:** simple-peer
- **UI:** Tailwind CSS
- **Сборка:** Vite

### Инфраструктура

- **Контейнеризация:** Docker
- **Reverse Proxy:** Nginx / Caddy
- **Кеширование:** Redis (опционально)

## WebSocket протокол

### Клиент → Сервер

**Присоединение к комнате:**
```json
{
  "type": "join_room",
  "payload": {
    "roomId": "abc123",
    "nickname": "User123",
    "password": "hash"
  }
}
```

**Управление воспроизведением (только хост):**
```json
{
  "type": "playback_command",
  "payload": {
    "command": "play | pause | seek | setRate",
    "value": 120.5
  }
}
```

**Сообщение в чат:**
```json
{
  "type": "chat_message",
  "payload": {
    "text": "Hello!"
  }
}
```

### Сервер → Клиент

**Подтверждение подключения:**
```json
{
  "type": "joined",
  "payload": {
    "clientId": "your-uuid",
    "roomState": { /* RoomState */ },
    "participants": [ /* Participant[] */ ]
  },
  "timestamp": 1700000000000
}
```

**Обновление воспроизведения:**
```json
{
  "type": "playback_update",
  "payload": {
    "state": "playing",
    "offset": 120.5,
    "rate": 1.0
  },
  "timestamp": 1700000000000
}
```

Полное описание протокола см. в [TECHNICAL_SPECIFICATION.md](./TECHNICAL_SPECIFICATION.md#8-api-и-протоколы)

## Roadmap

### MVP (v0.1) - Базовая функциональность
- [x] Техническое задание
- [x] План реализации
- [ ] WebSocket сервер
- [ ] Управление комнатами
- [ ] Синхронизация YouTube
- [ ] Текстовый чат
- [ ] Базовый UI

### v0.2 - Расширенная функциональность
- [ ] Голосовой чат (WebRTC)
- [ ] Поддержка VK Video
- [ ] Загрузка файлов
- [ ] Приватные комнаты

### v0.3 - Оптимизация
- [ ] Redis кеширование
- [ ] Rate limiting
- [ ] Метрики и мониторинг
- [ ] Unit и E2E тесты

### v1.0 - Production готовность
- [ ] Полное покрытие тестами
- [ ] Docker deployment
- [ ] HTTPS
- [ ] Документация API
- [ ] CI/CD

## Разработка

### Запуск тестов

**Серверные unit-тесты:**
```bash
cd server
npm test
```

**Клиентские E2E тесты:**
```bash
cd client
npm run test:e2e
```

### Линтинг

```bash
# Сервер
cd server
npm run lint

# Клиент
cd client
npm run lint
```

### Сборка

**Сервер:**
```bash
cd server
npm run build
```

**Клиент:**
```bash
cd client
npm run build
```

## Производительность

### Целевые метрики

- **Drift между участниками:** < 300мс (95-й перцентиль)
- **Время отклика на команду:** < 200мс
- **Uptime:** > 99%
- **Участников в комнате:** до 20
- **Одновременных комнат:** до 1000

### Требования к ресурсам

**Сервер (100 комнат):**
- CPU: < 50%
- RAM: < 100MB
- Трафик: < 10KB/сек на участника (без медиа)

## Безопасность

- **Rate limiting:** 10 команд/сек на пользователя
- **Валидация входящих данных:** zod схемы
- **XSS защита:** санитизация HTML в чате
- **Изоляция комнат:** доступ только по roomId
- **WebRTC шифрование:** DTLS

## Лицензия

MIT License

## Контакты

- GitHub Issues: для багов и предложений
- Документация: см. `/docs` директорию

---

**Статус проекта:** 📝 Проектирование завершено, готов к разработке

**Версия документации:** 1.0
**Дата:** 22.01.2026
